namespace CodeBase.UI.Windows {
    public class WordsListWindow : WindowBase {
        protected override void Initialize() =>
            UpdateList();
        
        private void UpdateList() {
            //TODO: Show words list
        }
    }
}